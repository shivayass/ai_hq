## .env.example

```
# Hugging Face token (free tier)
HF_TOKEN=hf_xxx_your_token_here

# Encryption key for memory (set this in Render env for security)
# Must be urlsafe base64 32 bytes. Example generation (locally):
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Toggle auto-deploy of generated skills (DANGER: keep false unless you know what you are doing)
ALLOW_AUTO_DEPLOY=false

# Optional: which HF model to use
HF_MODEL=gpt2
```
